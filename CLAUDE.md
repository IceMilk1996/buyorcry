@AGENTS.md

# chart-game

종목명과 시기를 가린 과거 차트를 한 봉씩 넘기며 매매하고,
끝나면 "그냥 들고 있었을 때"와 비교당하는 웹 게임. 기획서는 `README.md` 참조.

## 깨면 안 되는 설계 원칙

1. **게임 로직은 순수 함수로 유지한다.** (`src/lib/game/engine.ts`)
   렌더링 없이 수천 판을 돌릴 수 있어야 밸런싱 검증이 가능하다.
   여기에 React·DOM·fetch가 들어오는 순간 `npm run simulate` 가 죽는다.

2. **점수는 절대 수익률이 아니라 초과수익(알파)이다.**
   폭등 구간이 걸린 사람이 가만히 있어도 이기는 걸 막는다.

3. **미래 캔들을 클라이언트로 보내지 않는다.**
   개발자도구 네트워크 탭에 그대로 보인다. 턴마다 서버에서 하나씩 주고,
   자산 계산은 서버가 소유한다.

4. **일봉과 주봉은 모든 임계값을 따로 가져간다.**
   주봉은 구조적으로 변동성이 크다. 한 기준으로 재면 주봉이 전부 HARD로 몰리고,
   미조정 주가 탐지에서는 정상 구간이 오탐으로 탈락한다.

## 커밋 규칙

### 메시지 형식

```
<타입>: <한국어 요약>

<본문 — 왜 바꿨는지>

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
```

### 타입

| 타입 | 쓰는 경우 |
|------|-----------|
| `feat` | 새 기능 추가 |
| `fix` | 버그 수정 |
| `balance` | 필터·임계값·게임 수치 조정 (아래 별도 규칙) |
| `data` | 수집 스크립트, 데이터 파이프라인 (`scripts/*`) |
| `ui` | 화면 레이아웃·스타일 조정 (동작 변화 없음) |
| `refactor` | 동작은 그대로, 코드 구조만 변경 |
| `docs` | README, 주석, 문서 |
| `chore` | 의존성, 설정, 빌드 스크립트 |

### 제목

- **한국어**로 쓴다
- 50자 이내, 끝에 마침표를 붙이지 않는다
- "무엇을 했는지"가 드러나게 쓴다 — `fix: 버그 수정` ❌ / `fix: 주봉이 전부 HARD로 분류되던 문제 수정` ✅
- 여러 변경을 `·`나 `+`로 이어 붙이지 않는다. 그래야 할 것 같으면 커밋을 나눈다

### 본문

- **무엇을 바꿨는지가 아니라 왜 바꿨는지**를 적는다. 무엇은 diff가 이미 말해준다
- 증상이 있던 수정이라면 원인을 한 줄 남긴다
- 한 줄 72자 정도에서 줄바꿈, 항목이 여럿이면 `-` 목록
- 제목만으로 충분하면 본문은 생략해도 된다

### `balance` 커밋은 수치 근거가 필수

`FILTERS`, `DIFFICULTY_THRESHOLDS`, `INITIAL_CAPITAL`, `FEE_RATE`,
`REVEAL_COUNT`, `PLAY_COUNT` 를 바꾸는 커밋은 **왜 그 값인지가 본문에 없으면 안 된다.**
가능하면 `npm run simulate` 의 변경 전후 수치를 함께 적는다.

이 프로젝트는 수치 하나가 게임 재미를 좌우한다. 반년 뒤에 "왜 0.0419였지"를
diff만 보고 알 수 없으면 그 값은 다시 못 건드린다.

### 커밋 단위

- **한 커밋 = 한 가지 변경.** 기능 추가와 리팩터링을 한 커밋에 섞지 않는다
- 관련 없는 파일이 함께 스테이징되지 않았는지 `git diff --cached --name-only`로 확인한다
- 포맷팅만 바뀐 변경은 따로 커밋한다 (섞이면 리뷰가 불가능해진다)

## 커밋 전 확인

1. `npx tsc --noEmit` 통과 — 타입 에러가 있으면 커밋하지 않는다

   > `src/app/layout.tsx` 의 `Cannot find name 'LayoutProps'` 는 Next.js가
   > `.next/types` 에 생성하는 전역 타입이다. **`npm run dev` 를 한 번 띄운 적이
   > 없으면 뜬다.** 이 에러 하나만 남았다면 코드 문제가 아니므로,
   > 개발 서버를 한 번 실행한 뒤 다시 확인한다.
2. **`data/series/` 가 스테이징되지 않았는지 확인** — 수집 데이터 54MB.
   재현 가능하므로 저장소에 넣지 않는다. 한 번 푸시되면 히스토리에서 빼기 어렵다
3. `.env`, `.env.local`, API 키·토큰이 포함되지 않았는지 확인
4. `node_modules/`, `.next/`, `.venv/`, `__pycache__/` 가 섞이지 않았는지 확인

## 명령

```bash
npm run dev              # 개발 서버
npm run simulate         # 필터·전략 검증 (재보정용 변동성 분위수 포함)
npm run check:picker     # 문제 뽑기의 봉간격·난이도 분포
npm run probe:intervals  # 봉 간격별 변동성 실측
npm run fetch            # 코스피200 수집 (⚠️ 로컬 터미널에서만 — 아래 참조)
```

**데이터 수집은 Claude가 대신 실행할 수 없다.** 네이버·KRX 접근이
Cowork 작업환경과 클라우드 양쪽에서 차단되어 있다. 사용자가 직접 돌려야 한다.
