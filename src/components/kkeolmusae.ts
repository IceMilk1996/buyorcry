/**
 * 껄무새의 도형 — 로고와 마스코트가 같은 좌표를 쓴다.
 *
 * 한 군데서만 정의하는 이유: 로고와 게임 속 캐릭터가 미세하게 다르면
 * 같은 캐릭터로 안 읽힌다. 표정만 바뀌고 실루엣은 언제나 같아야 한다.
 *
 * 좌표계는 viewBox="0 0 96 96".
 */

/** 몸통 */
export const BODY = { x: 28, y: 15, w: 54, h: 66, r: 26 };

/**
 * 위턱 — 짧고 깊게 말리는 갈고리.
 * 이 선 하나가 앵무새를 앵무새로 만든다. 실루엣 밖으로 튀어나와야
 * 16px 에서도 새로 읽힌다 (안쪽 디테일은 다 버려도 된다).
 */
export const BEAK =
  'M47 28 C35 28, 23 32, 17 40 C13 46, 14 55, 20 56 C24 56, 24 51, 27 48 C33 43, 41 43, 47 44 Z';

/**
 * 입 — 벌린 정도가 곧 표정이다.
 *
 * ⚠️ 네 개 모두 **윗변이 같다**: (45,45) 에서 (23,51.5) 로 내려오는 곡선.
 *    이 선이 곧 부리의 아랫면이라, 여기가 흔들리면 표정마다 부리 모양이
 *    달라 보인다. 실제로 처음엔 크게 벌린 입이 부리를 파먹어서 대박일 때만
 *    부리가 얇았다. 벌어지는 건 아래쪽뿐이어야 한다.
 */
const LIP = 'M45 45 C36 44, 28 47, 23 51.5';

export const MOUTH = {
  /** 다물었다 */
  shut: `${LIP} C21.5 53, 22.5 55.5, 26 55 C32 51.5, 39 50, 45 49.5 Z`,
  /** 조금 벌렸다 */
  small: `${LIP} C20 55, 22 61, 27 62 C33 63.5, 41 60, 44 55 C46 51, 46 47, 45 45 Z`,
  /** 기본 — "껄!" */
  open: `${LIP} C20 56, 21 63, 27 66 C34 69, 42 66, 45 60 C47 54, 46 49, 45 45 Z`,
  /** 크게 — "껄!!!" */
  wide: `${LIP} C18 58, 19 68, 27 71 C36 74, 45 70, 47 61 C49 53, 47 48, 45 45 Z`,
} as const;

export const EYE = { cx: 58, cy: 37, r: 6.5 };
/** 웃는 눈 — 위로 휘었다. 마스크에서 선으로 뚫는다 */
export const EYE_SQUINT = 'M52.5 39 Q58 32.5 63.5 39';
/** 우는 눈 — 아래로 휘었다. 같은 선을 뒤집은 것뿐인데 정반대로 읽힌다 */
export const EYE_SAD = 'M52.5 33 Q58 39.5 63.5 33';
/** 눈물·식은땀 방울 */
export const drop = (x: number, y: number) => `M${x} ${y} q4 6 0 9 q-4 -3 0 -9z`;
